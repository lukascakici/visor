dependencies:
  idf: ">=5.1"
  # The round 1.28 inch panel's controller. Not in the IDF tree; swapped for
  # another driver when the panel changes, alongside visor_panel.c.
  espressif/esp_lcd_gc9a01: "^2.0.0"
