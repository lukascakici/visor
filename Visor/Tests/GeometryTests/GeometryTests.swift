import XCTest
@testable import Geometry

// Skeleton smoke test: is the package wired up and the test target alive?
final class GeometrySmokeTests: XCTestCase {
    func testModuleBuilds() {
        XCTAssertTrue(true)
    }
}
