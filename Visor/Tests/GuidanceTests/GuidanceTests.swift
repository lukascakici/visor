import XCTest
@testable import Guidance

// Skeleton smoke test: is the package wired up and the test target alive?
final class GuidanceSmokeTests: XCTestCase {
    func testModuleBuilds() {
        XCTAssertTrue(true)
    }
}
